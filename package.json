{
  "name": "short-css-vars-repo",
  "version": "0.0.0",
  "description": "Optimize CSS variables names",
  "private": true,
  "scripts": {
    "test": "lerna run test --concurrency=1 --stream",
    "build": "lerna run build",
    "docs": "lerna run docs",
    "bootstrap": "lerna bootstrap --hoist --nohoist=postcss && npm run build && npm run docs",
    "clean": "lerna clean --yes && rimraf node_modules",
    "postinstall": "npm run bootstrap"
  },
  "keywords": [
    "postcss",
    "postcss-plugin",
    "babel",
    "babel-plugin",
    "css",
    "variables",
    "custom",
    "properties",
    "short",
    "hash",
    "name",
    "shorten"
  ],
  "author": "@julians",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/zeitonline/short-css-vars.git"
  },
  "bugs": {
    "url": "https://github.com/zeitonline/short-css-vars/issues"
  },
  "homepage": "https://github.com/zeitonline/short-css-vars#readme",
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2"
  }
}
